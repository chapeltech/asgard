services:
  kdc:
    image: asgard/kdc
    hostname: kdc.example.com
    command: start-kdc
    ports: ["88/udp", "88/tcp", "464/tcp", "2666/tcp"]
    # In production, we'll have a persistent volume:
#    volumes:
#      - kdc1:/var/heimdal

#volumes:
#  kdc1:
